# Optional WebSocket URL used by the Connection Status hook for active connectivity checks.
# If not provided, the status bar will rely solely on browser online/offline events.
# Example:
# REACT_APP_WS_URL=wss://your-server.example.com/realtime
REACT_APP_WS_URL=
